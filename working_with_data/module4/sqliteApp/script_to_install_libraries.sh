npm install expo-sqlite # here, we need to install the specific version as mentioned in the starter code (version "~10.3.0", because in newer versions SQLite.openDatabase is depracated
# We can do this by manually editing the package.json file, removing package-lock.json and the node modules, and running 
npm install # on the edited json 

# Or directly run
npm install expo-sqlite@~10.3.0

npm install react-native-paper
